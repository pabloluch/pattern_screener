# render.yaml
services:
  - type: web
    name: wave-pattern-screener
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.12
    healthCheckPath: /health